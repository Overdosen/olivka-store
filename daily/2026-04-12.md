# Daily Log: 2026-04-12

## Sessions

### Session (12:35) - Automated Inventory & Category Stock

**Context:** The user wanted to automate stock management so that the total `stock` always equals the sum of all size quantities. Additionally, requested stock tracking at the category level.

**Key Exchanges:**
- User requested automatic calculation of product stock from sizes.
- Suggested and implemented adding `total_stock` to categories.
- Realized a DB trigger is the best way to keep category stock in sync even during orders.

**Decisions Made:**
- **Product Stock Automation**: Updated `ProductFormClient.jsx` to auto-calculate `stock` and make the field read-only if sizes are defined.
- **Category Stock Tracking**: Added `total_stock` column to `categories` table.
- **Database Triggers**: Implemented `trg_update_category_stock` in PostgreSQL to ensure category totals stay accurate after product updates or order completions.
- **New Admin Page**: Created `/admin/categories` for quick inventory oversight.

**Lessons Learned:**
- Using JSONB functions in Postgres (`jsonb_array_elements`) is efficient for aggregating quantities within JSON structures.
- DB Triggers are more reliable for cross-table counts than application-level logic when using a client-side SDK (Supabase) across multiple components.

**Action Items:**
- [x] Sync existing products and categories via SQL.
- [x] Automate admin form stock logic.
- [x] Create Categories overview page.
- [x] Add total units stat to Dashboard.
